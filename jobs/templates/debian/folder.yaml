- job-template:
    name: 'debian/packages/{name}'
    project-type: folder
    display-name: '{display-name}'

- job-template:
    name: 'debian/deps/{name}'
    project-type: folder
    display-name: '{display-name}'

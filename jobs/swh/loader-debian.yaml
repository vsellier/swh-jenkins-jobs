- project:
    name: DLDDEB
    display-name: swh-loader-debian
    pkg: loader.debian
    jobs:
      - "{name}"
      - "{name}/incoming-tag"
      - "{name}/pypi-upload"
      - "{name}/tests"
      - "{name}/tox"

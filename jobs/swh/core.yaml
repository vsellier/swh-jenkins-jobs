- project:
    name: DCORE
    display-name: swh-core
    pkg: core
    jobs:
      - "{name}"
      - "{name}/tests"
      - "{name}/tox"
      - "debian/package/{name}"
      - "debian/package/{name}/pipeline"

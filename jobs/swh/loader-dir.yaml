- project:
    name: DLDDIR
    display-name: swh-loader-dir
    pkg: loader.dir
    jobs:
      - "{name}"
      - "{name}/incoming-tag"
      - "{name}/pypi-upload"
      - "{name}/tests"
      - "{name}/tox"

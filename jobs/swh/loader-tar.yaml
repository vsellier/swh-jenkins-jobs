- project:
    name: DLDTAR
    display-name: swh-loader-tar
    pkg: loader.tar
    jobs:
      - "{name}"
      - "{name}/incoming-tag"
      - "{name}/pypi-upload"
      - "{name}/tests"
      - "{name}/tox"

- project:
    name: DLDSVN
    display-name: swh-loader-svn
    pkg: loader.svn
    jobs:
      - "{name}"
      - "{name}/incoming-tag"
      - "{name}/pypi-upload"
      - "{name}/tests"
      - "{name}/tox"

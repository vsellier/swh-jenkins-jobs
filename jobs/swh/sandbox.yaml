- project:
    name: TSBX
    display-name: swh-sandbox
    pkg: sandbox
    jobs:
      - "{name}"
      - "{name}/incoming-tag"
      - "{name}/tests"
      - "{name}/tox"
      - "{name}/pypi-upload"
      - "debian/packages/{name}"
      - "debian/packages/{name}/update-for-release"

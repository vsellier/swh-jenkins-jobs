- project:
    name: DSCH
    display-name: swh-scheduler
    pkg: scheduler
    jobs:
      - "{name}"
      - "{name}/incoming-tag"
      - "{name}/pypi-upload"
      - "{name}/tests"
      - "{name}/tox"
